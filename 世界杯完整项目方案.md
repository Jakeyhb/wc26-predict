52天内，"完善"的第一版应该能做到：



数据管道稳定，历史数据和实时数据都能跑

预测模型有回测验证，不是拍脑袋

新闻情报能自动抓取、LLM提取、人工审核

前端页面设计精良，移动端体验好

有赛后自动复盘机制

有管理后台能审核内容



不应该包含的： Kafka、ClickHouse、K8s、learned impact features、赛中实时预测。这些是第二版的事。



完整的7周计划明白了。你要的是一个完整、可信、值得上线的产品，不是能跑的Demo。这个目标是对的，52天完全可以做到，但需要清晰的架构和正确的执行顺序。



先看整体架构，所有模块都在里面：

&#x20; ::view-transition-group(\*),

&#x20; ::view-transition-old(\*),

&#x20; ::view-transition-new(\*) {

&#x20;   animation-duration: 0.25s;

&#x20;   animation-timing-function: cubic-bezier(0.19, 1, 0.22, 1);

&#x20; }

VvisualizeVvisualize show\_widget

7周计划（按模块，不按天）

五个模块按顺序开发，每个模块结束时有一个可验证的结果。

第1周（4/20-4/27）：项目骨架 + 数据库

结束时能跑起来：Docker一键启动，数据库建好，表结构完整。

第2周（4/28-5/4）：数据管道

结束时能看到：历史比赛数据进库，StatsBomb赛事数据加载完毕。

第3周（5/5-5/11）：预测引擎

结束时能看到：输入任意两队，输出胜平负概率 + Top3比分 + 置信度。

第4周（5/12-5/18）：新闻 + LLM流水线

结束时能看到：自动抓新闻，LLM提取信号，人工审核后台可用。

第5周（5/19-6/1）：前端 + 文章生成

结束时能看到：单场预测页完整，三次更新时间线，中文分析短文。

第6周（6/2-6/10）：联调 + 回测 + 软启动

结束时能看到：历史回放验证，赛后自动复盘跑通，首批内容上线。

第7周起（6/11-7/19）：正式运营

每场比赛自动存快照，赛后自动出Brier Score复盘表。



给Claude Code的5个完整提示词

每个提示词对应一个模块，按顺序执行。



提示词一：项目骨架 + 数据库

帮我搭建一个世界杯足球预测平台的完整项目骨架。

技术栈：Python 3.11 + FastAPI + SQLAlchemy + Alembic + PostgreSQL + Redis + Celery



=== 项目结构 ===



worldcup-predictor/

├── backend/

│   ├── app/

│   │   ├── main.py

│   │   ├── config.py           # 所有环境变量统一管理

│   │   ├── database.py         # SQLAlchemy engine + session

│   │   ├── models/             # ORM模型

│   │   │   ├── \_\_init\_\_.py

│   │   │   ├── team.py

│   │   │   ├── player.py

│   │   │   ├── match.py

│   │   │   ├── news\_article.py

│   │   │   ├── news\_signal.py

│   │   │   ├── prediction\_run.py

│   │   │   ├── postmatch\_eval.py

│   │   │   ├── content\_article.py

│   │   │   └── source\_registry.py

│   │   ├── routers/

│   │   │   ├── matches.py

│   │   │   ├── predictions.py

│   │   │   ├── signals.py

│   │   │   └── admin.py

│   │   └── services/           # 业务逻辑（空文件占位）

│   ├── alembic/

│   ├── tests/

│   ├── requirements.txt

│   └── Dockerfile

├── frontend/                   # 占位，后续实现

├── docker-compose.yml

└── .env.example



=== 数据库表结构 ===



严格按照以下规范实现所有ORM模型：



【teams表】

\- id: UUID主键

\- name: varchar(100) 英文名

\- name\_zh: varchar(100) 中文名

\- fifa\_code: varchar(3) 三字母代码（BRA, FRA等）

\- confederation: varchar(10) 洲际联合会（UEFA, CONMEBOL等）

\- elo\_rating: float 默认1500

\- created\_at, updated\_at



【players表】

\- id: UUID主键

\- team\_id: FK → teams

\- name: varchar(100)

\- name\_zh: varchar(100)

\- position: varchar(20) GK/DEF/MID/FWD

\- is\_key\_player: boolean 是否核心球员

\- current\_club: varchar(100)

\- created\_at, updated\_at



【matches表】

\- id: UUID主键

\- external\_id: varchar(50) 数据源原始ID（用于去重）

\- home\_team\_id, away\_team\_id: FK → teams

\- match\_date: timestamptz

\- competition: varchar(50) 赛事名

\- competition\_weight: float（世界杯=1.0，资格赛=0.7，友谊赛=0.4）

\- stage: varchar(50) 赛阶段（group/round\_of\_32等）

\- venue: varchar(100)

\- is\_neutral\_venue: boolean

\- status: varchar(20) scheduled/live/finished

\- created\_at, updated\_at



【match\_results表】（只有已结束比赛才有）

\- id: UUID主键

\- match\_id: FK → matches (unique)

\- home\_goals: int

\- away\_goals: int

\- home\_xg: float nullable

\- away\_xg: float nullable

\- created\_at



【news\_articles表】

\- id: UUID主键

\- external\_id: varchar(200) 来源URL hash，用于去重

\- source\_name: varchar(100)

\- source\_url: text

\- title: text

\- content: text

\- language: varchar(10)

\- published\_at: timestamptz

\- fetched\_at: timestamptz

\- is\_processed: boolean default false

\- created\_at



【news\_signals表】（从news\_articles提取的结构化信号）

\- id: UUID主键

\- article\_id: FK → news\_articles

\- match\_id: FK → matches nullable（关联到具体比赛）

\- team\_id: FK → teams nullable

\- signal\_type: varchar(30) 枚举：injury/return/lineup\_hint/

&#x20;                            coach\_statement/training/travel/weather/other

\- impact\_direction: varchar(10) 枚举：positive/negative/neutral/uncertain

\- confidence: float 0-1（LLM对信息可信度的判断）

\- key\_players: json array 涉及球员名列表

\- summary\_zh: varchar(200) 30字内中文摘要

\- source\_reliability: float 0-1 来源可信度评分

\- review\_status: varchar(20) 枚举：pending/approved/rejected

\- reviewed\_by: varchar(50) nullable

\- reviewed\_at: timestamptz nullable

\- enters\_model: boolean default false（审核通过后才能为true）

\- created\_at



【prediction\_runs表】（每场比赛每次预测存一个run）

\- id: UUID主键

\- match\_id: FK → matches

\- run\_type: varchar(20) 枚举：t\_minus\_24h/t\_minus\_3h/lineup\_confirmed

\- model\_version: varchar(20)

\- as\_of\_time: timestamptz（这次预测时刻，用于防数据泄漏）

\- home\_win\_prob: float

\- draw\_prob: float

\- away\_win\_prob: float

\- home\_xg: float

\- away\_xg: float

\- score\_matrix: jsonb（0:0到4:4的完整概率矩阵）

\- top3\_scores: jsonb（\[{"score":"1:0","prob":0.18}, ...]）

\- confidence\_score: float 0-1

\- risk\_tags: jsonb array（\["首发不确定","旅行疲劳"]等）

\- input\_feature\_snapshot: jsonb（记录用了哪些特征）

\- approved\_signals: jsonb array（记录哪些信号进入了模型）

\- created\_at



【postmatch\_eval表】（赛后自动填入）

\- id: UUID主键

\- prediction\_run\_id: FK → prediction\_runs

\- actual\_home\_goals: int

\- actual\_away\_goals: int

\- actual\_result: varchar(5) H/D/A

\- brier\_score: float

\- log\_loss: float

\- exact\_score\_hit: boolean

\- top3\_hit: boolean

\- calibration\_bucket: int 1-10

\- notes: text

\- created\_at



【content\_articles表】（AI生成的中文分析文章）

\- id: UUID主键

\- match\_id: FK → matches

\- prediction\_run\_id: FK → prediction\_runs

\- title: varchar(200)

\- body: text

\- article\_version: int default 1

\- is\_published: boolean default false

\- correction\_log: jsonb array（记录所有人工修改历史）

\- created\_at, updated\_at



【source\_registry表】（新闻来源可信度配置）

\- id: UUID主键

\- domain: varchar(200) 来源域名

\- source\_name: varchar(100)

\- reliability\_score: float 0-1

\- tier: int 1-5（1=官方, 2=主流媒体, 3=专业体育媒体, 4=地方/博客, 5=匿名）

\- notes: text

\- created\_at, updated\_at



=== FastAPI基础配置 ===



1\. 实现config.py，用pydantic-settings管理所有环境变量：

&#x20;  POSTGRES\_URL, REDIS\_URL, 

&#x20;  FOOTBALL\_DATA\_API\_KEY,

&#x20;  EVENT\_REGISTRY\_API\_KEY,

&#x20;  LLM\_PROVIDER（枚举：qwen/deepseek/zhipu）

&#x20;  LLM\_BASE\_URL, LLM\_API\_KEY, LLM\_MODEL,

&#x20;  OBJECT\_STORAGE\_BUCKET



2\. 实现database.py：async SQLAlchemy engine + get\_db依赖



3\. 实现main.py：FastAPI app，挂载所有router，加CORS



4\. 实现router骨架（每个router有占位的endpoint，

&#x20;  返回{"status": "not implemented yet"}）



=== Docker配置 ===



docker-compose.yml包含：

\- postgres:16（带healthcheck）

\- redis:7-alpine

\- backend（FastAPI，开发模式hot reload）



.env.example：包含所有必需的环境变量



=== 数据库迁移 ===



配置Alembic，生成第一个migration文件，

包含所有上述表的建表语句。



要求：所有代码有类型注解，所有model有完整的relationship定义。



提示词二：数据管道

在已有项目骨架基础上，实现数据采集和同步服务。



=== 1. football-data.org 数据服务 ===



实现 backend/app/services/football\_data\_service.py



使用football-data.org v4 API（Base URL: https://api.football-data.org/v4）

认证：Header X-Auth-Token



需要实现的方法：



async def fetch\_competition\_matches(competition\_code: str, season: int) -> list\[dict]

\# 拉取指定赛事指定赛季的所有比赛

\# competition\_code示例：WC（世界杯）、WCQ（资格赛）

\# 映射字段：id, utcDate, homeTeam, awayTeam, score, stage, status



async def fetch\_team\_squad(team\_id: int) -> dict

\# 拉取球队阵容信息



async def sync\_historical\_matches()

\# 同步以下历史数据到matches表和match\_results表：

\# - 2018 FIFA World Cup（WC赛事）

\# - 2022 FIFA World Cup（WC赛事）

\# - 近2年各队世界杯资格赛（按confederation分组）

\# 使用upsert，根据external\_id去重

\# 打印进度日志



async def sync\_upcoming\_matches()

\# 同步2026世界杯赛程（6月11日起）

\# 只同步未来30天内的比赛



=== 2. StatsBomb Open Data 加载器 ===



实现 backend/app/services/statsbomb\_service.py



StatsBomb Open Data GitHub:

https://raw.githubusercontent.com/statsbomb/open-data/master/data/



需要实现的方法：



async def load\_competitions() -> list\[dict]

\# 加载competitions.json，找到men's FIFA World Cup比赛



async def load\_matches(competition\_id: int, season\_id: int) -> list\[dict]

\# 加载对应赛季的所有比赛



async def load\_events(match\_id: int) -> list\[dict]

\# 加载单场比赛的所有事件（shot, pass, carry等）



async def extract\_team\_style\_features(team\_name: str, competition\_id: int, season\_id: int) -> dict

\# 从events数据中提取球队风格特征：

\# - avg\_shots\_per\_game: 场均射门

\# - avg\_shots\_on\_target: 场均射正

\# - avg\_xg: 场均xG（仅限shot类事件的statsbomb\_xg字段）

\# - avg\_xga: 场均被期望进球

\# - set\_piece\_threat: 定位球威胁（set\_piece类射门比例）

\# - pressing\_intensity: 压迫强度（defensive\_actions\_in\_opp\_half / 90min）

\# 返回: {team\_name, competition, season, features: {...}}



async def build\_training\_dataset() -> pd.DataFrame

\# 整合2018和2022世界杯所有比赛：

\# 输出包含以下字段的DataFrame：

\# match\_date, home\_team, away\_team, 

\# home\_goals, away\_goals, 

\# competition\_weight, is\_neutral\_venue,

\# home\_xg, away\_xg (来自events计算)



=== 3. Celery定时任务 ===



实现 backend/app/workers/celery\_app.py 和任务文件



配置Celery使用Redis作为broker和backend



任务1：sync\_matches\_task（每天04:00 UTC执行）

\- 调用sync\_upcoming\_matches()

\- 更新已结束比赛的比分



任务2：run\_predictions\_task（在比赛前T-24h和T-3h触发）

\- 扫描24小时内即将开始的比赛

\- 对每场比赛触发预测（预测引擎还未实现，先记录日志）



任务3：postmatch\_eval\_task（比赛结束后1小时触发）

\- 找到已结束但还没有postmatch\_eval记录的比赛

\- 自动计算Brier Score等指标并写入postmatch\_eval表



=== 4. 管理命令 ===



实现 backend/scripts/init\_data.py

执行步骤：

1\. 创建所有数据库表（Alembic upgrade head）

2\. 加载source\_registry基础数据（主要新闻来源的可信度配置）

3\. 同步StatsBomb历史数据

4\. 同步football-data.org历史比赛

5\. 打印数据统计：共加载X场比赛，X支球队



要求：所有API调用有retry机制（最多3次），有详细的日志输出。



提示词三：Dixon-Coles预测引擎

实现世界杯比赛预测引擎的核心部分。



=== 1. Dixon-Coles模型 ===



实现 backend/app/services/dixon\_coles.py



class DixonColesModel:

&#x20;   

&#x20;   def \_\_init\_\_(self):

&#x20;       self.attack\_params = {}    # {team\_name: float}

&#x20;       self.defense\_params = {}   # {team\_name: float}

&#x20;       self.home\_advantage = 0.0

&#x20;       self.rho = 0.0             # 低比分修正参数

&#x20;       self.trained\_at = None

&#x20;   

&#x20;   def \_time\_weight(self, match\_date: date, reference\_date: date, half\_life\_days=180) -> float:

&#x20;       """指数衰减时间权重，半衰期180天"""

&#x20;   

&#x20;   def \_tau(self, x: int, y: int, lambda\_: float, mu\_: float, rho: float) -> float:

&#x20;       """Dixon-Coles低比分修正项

&#x20;       只对(0,0),(1,0),(0,1),(1,1)四种比分有修正"""

&#x20;   

&#x20;   def \_log\_likelihood(self, params: np.ndarray, matches\_df: pd.DataFrame) -> float:

&#x20;       """完整的Dixon-Coles对数似然函数

&#x20;       参数顺序：\[attack\_params..., defense\_params..., home\_advantage, rho]

&#x20;       包含时间权重和赛事权重"""

&#x20;   

&#x20;   def fit(self, matches\_df: pd.DataFrame):

&#x20;       """用MLE估计所有参数

&#x20;       matches\_df必须包含：

&#x20;       match\_date, home\_team, away\_team, 

&#x20;       home\_goals, away\_goals,

&#x20;       competition\_weight, is\_neutral\_venue

&#x20;       

&#x20;       使用scipy.optimize.minimize，method='L-BFGS-B'

&#x20;       约束：所有attack/defense参数>0，-1<rho<1

&#x20;       返回训练摘要：参数数量、最终似然值、收敛状态"""

&#x20;   

&#x20;   def predict\_score\_matrix(

&#x20;       self, 

&#x20;       home\_team: str, 

&#x20;       away\_team: str, 

&#x20;       is\_neutral\_venue: bool = True,

&#x20;       max\_goals: int = 5

&#x20;   ) -> np.ndarray:

&#x20;       """返回(max\_goals+1) x (max\_goals+1)的比分概率矩阵

&#x20;       matrix\[i]\[j] = P(home\_goals=i, away\_goals=j)"""

&#x20;   

&#x20;   def predict\_match(

&#x20;       self, 

&#x20;       home\_team: str, 

&#x20;       away\_team: str,

&#x20;       is\_neutral\_venue: bool = True

&#x20;   ) -> dict:

&#x20;       """完整预测输出：

&#x20;       {

&#x20;           home\_win\_prob: float,

&#x20;           draw\_prob: float, 

&#x20;           away\_win\_prob: float,

&#x20;           home\_xg: float,        # 期望进球 = sum(i \* P(home=i))

&#x20;           away\_xg: float,

&#x20;           top3\_scores: \[         # 概率最高的3个比分

&#x20;               {"score": "1:0", "prob": 0.18},

&#x20;               ...

&#x20;           ],

&#x20;           score\_matrix: list\[list\[float]],   # 完整概率矩阵

&#x20;           model\_params\_used: {               # 透明度：用了哪些参数

&#x20;               "home\_attack": float,

&#x20;               "home\_defense": float,

&#x20;               "away\_attack": float,

&#x20;               "away\_defense": float,

&#x20;               "rho": float,

&#x20;               "home\_advantage\_applied": bool

&#x20;           }

&#x20;       }"""

&#x20;   

&#x20;   def evaluate(self, test\_matches\_df: pd.DataFrame) -> dict:

&#x20;       """回测评估

&#x20;       返回：

&#x20;       {

&#x20;           brier\_score: float,

&#x20;           log\_loss: float,

&#x20;           calibration: {          # 每个概率区间的实际命中率

&#x20;               "0.0-0.1": float,

&#x20;               "0.1-0.2": float,

&#x20;               ...

&#x20;           },

&#x20;           exact\_score\_hit\_rate: float,

&#x20;           top3\_hit\_rate: float

&#x20;       }"""

&#x20;   

&#x20;   def save(self, path: str): ...

&#x20;   def load(self, path: str): ...





=== 2. 信号规则修正器 ===



实现 backend/app/services/signal\_adjuster.py



class SignalAdjuster:

&#x20;   """

&#x20;   接收Dixon-Coles的基础预测结果，

&#x20;   根据已审核通过的news\_signals进行规则化调整。

&#x20;   

&#x20;   规则设计原则：

&#x20;   - 只有enters\_model=True的信号才能影响预测

&#x20;   - 调整量有上限（防止单一信号过度影响）

&#x20;   - 所有调整都有日志，可追溯

&#x20;   """

&#x20;   

&#x20;   # 各类信号的最大调整幅度（占原始xG的比例）

&#x20;   MAX\_ADJUSTMENT = {

&#x20;       "injury\_key\_striker": 0.15,       # 核心前锋确认缺阵：最多下调15%进攻xG

&#x20;       "injury\_key\_goalkeeper": 0.10,    # 主力门将缺阵：最多上调10%对方进球期望

&#x20;       "return\_key\_player": 0.08,        # 核心球员回归

&#x20;       "major\_rotation": 0.12,           # 大面积轮换

&#x20;       "travel\_fatigue": 0.06,           # 旅行疲劳

&#x20;   }

&#x20;   

&#x20;   def apply\_signals(

&#x20;       self,

&#x20;       base\_prediction: dict,        # Dixon-Coles原始输出

&#x20;       approved\_signals: list\[dict], # review\_status='approved' and enters\_model=True

&#x20;       match\_context: dict           # stage, rest\_days, must\_not\_lose等上下文

&#x20;   ) -> dict:

&#x20;       """

&#x20;       返回调整后的预测结果，包含：

&#x20;       - 原始字段（调整后的数值）

&#x20;       - adjustment\_log: 每条信号做了什么调整，调整幅度多少

&#x20;       - confidence\_adjustment: 信号数量和质量对置信度的影响

&#x20;       - risk\_tags: 根据信号生成的风险标签列表

&#x20;       """

&#x20;   

&#x20;   def \_calculate\_risk\_tags(self, signals: list, context: dict) -> list\[str]:

&#x20;       """

&#x20;       根据信号和上下文生成风险标签：

&#x20;       可能的标签：

&#x20;       - "首发不确定"（lineup\_hint信号confidence<0.6）

&#x20;       - "旅行疲劳"（travel信号存在）

&#x20;       - "关键球员缺阵"（injury信号针对key\_player）

&#x20;       - "小组赛轮换风险"（stage=group and matchday=3）

&#x20;       - "旅行距离较长"（context中travel\_km>3000）

&#x20;       """





=== 3. 预测编排器 ===



实现 backend/app/services/prediction\_orchestrator.py



class PredictionOrchestrator:

&#x20;   

&#x20;   async def run\_prediction(

&#x20;       self,

&#x20;       match\_id: UUID,

&#x20;       run\_type: str,           # t\_minus\_24h / t\_minus\_3h / lineup\_confirmed

&#x20;       db: AsyncSession

&#x20;   ) -> UUID:                   # 返回prediction\_run\_id

&#x20;       """

&#x20;       完整预测流程：

&#x20;       1. 从数据库加载比赛信息

&#x20;       2. 拉取该比赛两队的历史数据作为训练集

&#x20;       3. 实例化并训练Dixon-Coles模型

&#x20;       4. 获取该比赛as\_of\_time之前的所有已审核信号

&#x20;       5. 应用信号修正

&#x20;       6. 将结果写入prediction\_runs表，记录完整快照

&#x20;       7. 触发文章生成任务（异步，不阻塞预测）

&#x20;       8. 返回prediction\_run\_id

&#x20;       

&#x20;       注意：训练集只能包含as\_of\_time之前的比赛结果（防止数据泄漏）

&#x20;       """





=== 4. FastAPI预测接口 ===



在 backend/app/routers/predictions.py 实现：



GET /api/predictions/{match\_id}/latest

\# 返回这场比赛最新的预测结果（最近一个prediction\_run）

\# 包含：预测数据 + 使用的信号摘要 + 风险标签



GET /api/predictions/{match\_id}/history

\# 返回这场比赛所有run的历史（T-24h, T-3h, lineup\_confirmed）

\# 用于前端展示时间线



GET /api/matches/upcoming

\# 返回未来7天内的所有比赛，每场附带latest预测（如有）



POST /api/predictions/{match\_id}/trigger

\# 手动触发一次预测（管理员用）

\# body: {"run\_type": "lineup\_confirmed"}



要求：

\- 所有接口有完整的Pydantic response schema

\- 有详细的错误处理和日志

\- 写至少10个单元测试覆盖Dixon-Coles的核心计算逻辑



提示词四：新闻 + LLM流水线

实现新闻采集、LLM信号提取和审核后台。



=== 1. 新闻采集服务 ===



实现 backend/app/services/news\_ingest\_service.py



class NewsIngestService:

&#x20;   

&#x20;   async def fetch\_event\_registry(self, keywords: list\[str], hours\_back: int = 24) -> list\[dict]

&#x20;   # Event Registry API: https://eventregistry.org/api/v1/article/getArticles

&#x20;   # 关键词：球队名、球员名、"FIFA World Cup 2026"等

&#x20;   # 返回标准化的文章列表

&#x20;   

&#x20;   async def fetch\_gdelt(self, keywords: list\[str], hours\_back: int = 6) -> list\[dict]

&#x20;   # GDELT API: https://api.gdeltproject.org/api/v2/doc/doc

&#x20;   # 过滤：体育类别，语言：en/zh/es/pt/fr

&#x20;   

&#x20;   async def fetch\_rss\_feeds(self) -> list\[dict]

&#x20;   # 预配置的RSS源列表（FIFA官网、主要体育媒体）

&#x20;   # 从configs/rss\_sources.yaml加载源列表

&#x20;   

&#x20;   async def deduplicate\_and\_store(self, articles: list\[dict], db: AsyncSession) -> int

&#x20;   # 去重逻辑：URL hash作为external\_id

&#x20;   # 只存新文章，返回新增数量

&#x20;   

&#x20;   def calculate\_source\_reliability(self, source\_url: str, db: AsyncSession) -> float

&#x20;   # 查source\_registry表

&#x20;   # 未知来源默认0.3





=== 2. LLM信号提取服务 ===



实现 backend/app/services/llm\_service.py



首先实现统一LLM适配器基类：



class LLMAdapter(ABC):

&#x20;   async def chat(self, system: str, user: str, response\_format: str = "text") -> str: ...



class QwenAdapter(LLMAdapter): ...

class DeepSeekAdapter(LLMAdapter): ...

class ZhipuAdapter(LLMAdapter): ...



工厂函数：get\_llm\_adapter() -> LLMAdapter（根据LLM\_PROVIDER环境变量）



所有三个适配器都使用OpenAI兼容接口格式。

Qwen: base\_url = https://dashscope.aliyuncs.com/compatible-mode/v1

DeepSeek: base\_url = https://api.deepseek.com/v1  

Zhipu: base\_url = https://open.bigmodel.cn/api/paas/v4/



然后实现提取服务：



class SignalExtractorService:

&#x20;   

&#x20;   SYSTEM\_PROMPT = """你是一个足球情报分析师。

你的任务是从体育新闻中提取与世界杯相关的关键信号。

只提取明确陈述的信息，不要推断或猜测。

严格按照指定JSON格式输出，不要输出任何其他内容。"""



&#x20;   USER\_PROMPT\_TEMPLATE = """分析以下体育新闻，提取与世界杯有关的信号：



新闻标题：{title}

新闻内容：{content}

来源：{source\_name}

发布时间：{published\_at}



请提取所有相关信号，按如下JSON格式输出（数组，每个元素是一个信号）：

\[

&#x20; {{

&#x20;   "team\_mentioned": "球队英文名（如无法确认则null）",

&#x20;   "signal\_type": "injury|return|lineup\_hint|coach\_statement|training|travel|weather|other",

&#x20;   "impact\_direction": "positive|negative|neutral|uncertain",

&#x20;   "confidence": 0.0到1.0之间的浮点数（你对这条信息准确性的判断）,

&#x20;   "key\_players": \["球员名1", "球员名2"],

&#x20;   "summary\_zh": "不超过30字的中文摘要",

&#x20;   "source\_reliability\_hint": "official|major\_media|reporter|local\_media|unknown"

&#x20; }}

]



如果新闻与世界杯无关，返回空数组 \[]。"""



&#x20;   async def extract\_signals(self, article: NewsArticle, db: AsyncSession) -> list\[NewsSignal]

&#x20;   # 调用LLM提取信号

&#x20;   # 解析JSON输出（处理可能的格式错误）

&#x20;   # 查询teams表匹配team\_mentioned到team\_id

&#x20;   # 写入news\_signals表，review\_status='pending'

&#x20;   # 标记article.is\_processed=True

&#x20;   

&#x20;   async def process\_unprocessed\_articles(self, db: AsyncSession, batch\_size: int = 10)

&#x20;   # 批量处理is\_processed=False的文章

&#x20;   # 有rate limit保护（每分钟最多50次LLM调用）

&#x20;   

&#x20;   async def resolve\_conflicts(self, match\_id: UUID, signal\_type: str, db: AsyncSession) -> str

&#x20;   # 当同一比赛同一类型有多条互相矛盾的信号时

&#x20;   # 调用LLM做仲裁，返回仲裁结论

&#x20;   # 例：A记者说梅西缺阵，B记者说梅西出战 → LLM判断哪个更可信





=== 3. 管理后台API ===



实现 backend/app/routers/admin.py



以下接口全部要求Bearer token认证（简单实现，ADMIN\_TOKEN环境变量）：



GET /api/admin/signals/pending

\# 返回所有review\_status='pending'的信号

\# 支持按match\_id、team\_id、signal\_type过滤

\# 分页：page, page\_size



PATCH /api/admin/signals/{signal\_id}/review

\# body: {"status": "approved"|"rejected", "enters\_model": bool, "notes": str}

\# 批量更新：支持传入signal\_ids列表



GET /api/admin/articles/pending

\# 返回待审核的content\_articles（is\_published=False）



PATCH /api/admin/articles/{article\_id}/publish

\# 发布文章，同时记录correction\_log



GET /api/admin/dashboard

\# 返回运营数据：

\# - 今日新增文章数

\# - 待审核信号数

\# - 今日预测运行数

\# - 最近5场比赛的预测准确率（如有历史数据）



POST /api/admin/predictions/{match\_id}/trigger

\# 手动触发预测



=== 4. 文章生成服务 ===



实现 backend/app/services/article\_generator.py



class ArticleGeneratorService:

&#x20;   

&#x20;   ARTICLE\_SYSTEM\_PROMPT = """你是一名专业的足球分析记者。

根据提供的预测数据和情报信号，撰写简洁、准确的赛前分析文章。

使用数据支撑观点，不做过度推测，不使用博彩化语言。

文章结构固定：结论段 → 支撑论据 → 风险提示 → 更新说明。"""



&#x20;   async def generate\_article(

&#x20;       self,

&#x20;       prediction\_run: PredictionRun,

&#x20;       approved\_signals: list\[NewsSignal],

&#x20;       db: AsyncSession

&#x20;   ) -> ContentArticle:

&#x20;   # 构建包含预测数据+信号摘要的prompt

&#x20;   # 明确告知LLM：不能编造信息，只能使用provided data

&#x20;   # 文章长度：300-500字

&#x20;   # 写入content\_articles表，is\_published=False（等待人工审核）



要求：LLM调用全部有超时（30秒）和重试（最多2次）。



提示词五：React前端

实现世界杯预测平台的前端。

技术栈：React 18 + Vite + Tailwind CSS + Lucide React

API基础URL从环境变量VITE\_API\_BASE\_URL读取。



=== 设计规范 ===



颜色系统（使用CSS变量）：

\--bg-primary: #0a0a0f        深黑背景

\--bg-card: #111118           卡片背景

\--bg-elevated: #1a1a24       悬浮/高亮

\--border: #2a2a3a            边框

\--text-primary: #f0f0f5      主文字

\--text-secondary: #8888a0    次要文字

\--text-muted: #555568        说明文字

\--accent-blue: #4f8ef7       蓝色强调（主队/主色）

\--accent-green: #3dba7e      绿色（胜利/正向信号）

\--accent-amber: #f5a623      橙色（警告/风险标签）

\--accent-red: #e8514a        红色（负向信号）



字体：Syne（标题/数据）+ Inter（正文）

引入方式：Google Fonts



=== 页面结构 ===



实现以下3个页面：



【页面1：首页 /】

\- 顶部：logo + 导航

\- 今日/近期比赛列表（卡片形式，每张卡片显示：

&#x20; 两队名+国旗emoji、比赛时间、胜平负概率条、置信度标签）

\- 点击卡片跳转到单场预测页



【页面2：单场预测页 /match/:matchId】

这是最核心的页面，包含以下区块：



区块A：比赛头部

\- 两队名称（大字）、国旗emoji、比赛时间、赛事阶段

\- 更新时间线：T-24h ✓ / T-3h ✓ / 首发后 ○（当前所在阶段高亮）



区块B：预测核心数据（3列布局）

\- 左列：主队胜率（大数字，蓝色，带弧形进度条）

\- 中列：平局概率（中等大小，灰色）

\- 右列：客队胜率（大数字，反向色）



区块C：Top 3 预期比分

\- 3个卡片横排：比分 + 概率百分比

\- 第1名卡片加金色边框高亮



区块D：期望进球对比

\- 双向水平条形图：主队xG vs 客队xG

\- 数字精确到小数点后2位



区块E：情报信号列表

\- 每条信号：类型图标 + 摘要 + 影响方向（颜色编码）+ 来源可信度小圆点

\- 最多显示5条，超过有"展开"按钮

\- 未审核的信号不显示（只显示review\_status='approved'的）



区块F：风险标签

\- 小标签形式横排：如"首发不确定"、"旅行疲劳"等

\- 橙色背景，圆角



区块G：AI分析文章

\- 可折叠，默认展开

\- 显示文章正文

\- 底部：生成时间 + "发现错误？点击反馈"



区块H：历史预测对比（如有多个run）

\- 小型时间线：T-24h预测 vs T-3h预测 vs 首发后预测

\- 显示胜平负概率的变化方向



【页面3：赛后复盘页 /match/:matchId/review】

\- 实际比分 vs 预测比分的对比展示

\- 各次预测的Brier Score

\- 信号哪些准确了、哪些误导了

\- 简洁的表格形式



=== 组件要求 ===



所有组件：

\- 移动端优先，最大宽度600px，居中显示

\- 骨架屏加载状态（Skeleton组件）

\- 空状态处理（比赛还没有预测数据时的占位）

\- 使用React Query（@tanstack/react-query）管理API请求和缓存



动效：

\- 胜平负概率条：从0动画到目标值（CSS transition, 0.8s ease-out）

\- 卡片：hover时边框变亮（--border → --bg-elevated）

\- 信号列表展开/收起：高度动画



数据格式假设（按这个格式调用API）：

GET /api/matches/upcoming → 比赛列表

GET /api/predictions/{matchId}/latest → 最新预测

GET /api/predictions/{matchId}/history → 预测历史



用Mock数据实现，让UI在API未完成时可以独立预览。

Mock数据要真实：用真实的世界杯球队名和合理的概率数值。

